# Grand Horizon Hotel Management

## Configuration
- `GEMINI_API_KEY`: Required for any AI-powered report summaries (optional).
- `JWT_SECRET`: Secret for admin authentication sessions.

## Setup
1. Standard environment setup handles dependencies.
2. The SQLite database is automatically initialized on first run.
